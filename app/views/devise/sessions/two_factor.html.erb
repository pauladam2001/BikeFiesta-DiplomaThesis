<%= form_for(resource, as: resource_name, url: session_path(resource_name), method: :post) do |f| %>
  <%= f.text_field :code, autofocus: true, autocomplete: "code", placeholder: "Code" %>
  <%= f.submit "Sign in", class: "button" %>
<% end %>
