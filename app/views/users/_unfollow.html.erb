<%= form_with(model: current_user.active_relationships.find_by(followed_id: user.id), html: { method: :delete }, local: true) do |f| %>
  <%= f.submit "Unfollow", class: "button", style: "padding: 5px 10px; font-size: 14px; text-transform: none; font-size: 12px;", data: { turbo: "false" } %> 
<% end %>